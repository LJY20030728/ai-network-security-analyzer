# ===== 核心依赖（纯Python包，无需编译，Python 3.13兼容） =====
# Web框架
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9

# 网络抓包与协议分析（纯Python）
scapy>=2.5.0
dpkt>=1.9.8

# 大模型与AI
langchain>=0.3.0
langchain-openai>=0.2.0
langchain-community>=0.3.0
openai>=1.30.0
chromadb>=0.5.0
tiktoken>=0.7.0
# BGE 中文嵌入模型的 ONNX 本地推理（RAG 检索，离线运行，无需 API）
onnxruntime>=1.17.0

# 机器学习 / 数值计算 / 绘图（项目核心代码直接 import，显式声明以防间接依赖变动）
scikit-learn>=1.4.0
numpy>=1.26.0
pandas>=2.0.0
matplotlib>=3.8.0
joblib>=1.3.0

# 工具库
python-dotenv>=1.0.0
pydantic>=2.9.0
pydantic-settings>=2.4.0
loguru>=0.7.0
tqdm>=4.66.0
aiohttp>=3.10.0
httpx>=0.27.0

# 报告导出（Word / 读取 PDF 资料）
python-docx>=1.1.0
PyPDF2>=3.0.0

# 前端界面（简单Web UI，无需学React）
gradio>=4.40.0

# 桌面窗口化（原生应用窗口，而非浏览器）
pywebview>=5.0.0

# 图标/图像处理（应用图标生成与界面）
pillow>=10.0.0

# 测试
pytest>=8.3.0
