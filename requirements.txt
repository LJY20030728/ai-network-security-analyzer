# ===== 核心依赖（纯Python包，无需编译，Python 3.13兼容） =====
# Web框架
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9

# 网络抓包与协议分析（纯Python）
scapy>=2.5.0
dpkt>=1.9.8

# 大模型与AI
langchain>=0.3.0
langchain-openai>=0.2.0
langchain-community>=0.3.0
chromadb>=0.5.0
tiktoken>=0.7.0

# 工具库
scikit-learn>=1.4.0
python-dotenv>=1.0.0
pydantic>=2.9.0
pydantic-settings>=2.4.0
loguru>=0.7.0
tqdm>=4.66.0
aiohttp>=3.10.0
httpx>=0.27.0

# 前端界面（简单Web UI，无需学React）
gradio>=4.40.0

# 桌面窗口化（原生应用窗口，而非浏览器）
pywebview>=5.0.0

# 图标/图像处理（应用图标生成与界面）
pillow>=10.0.0

# 测试
pytest>=8.3.0
