# 网络安全基础知识测试文档

## 一、常见网络攻击类型

### 1.1 DDoS攻击
DDoS（分布式拒绝服务攻击）是指攻击者利用大量受控主机向目标服务器发送海量请求，导致服务器资源耗尽，无法为正常用户提供服务。

**常见类型：**
- **UDP Flood**：发送大量UDP数据包，耗尽服务器带宽
- **SYN Flood**：发送大量SYN请求，但不完成三次握手，耗尽连接队列
- **ICMP Flood**：发送大量ICMP Echo请求（Ping），耗尽带宽

**检测特征：**
- 短时间内来自大量不同IP的请求
- 目标端口集中，流量异常增大
- 连接数异常高，正常连接建立率低

### 1.2 端口扫描
端口扫描是攻击者探测目标主机开放端口和服务的常用手段。

**常见类型：**
- **TCP SYN扫描**：发送SYN包，不完成三次握手
- **TCP Connect扫描**：完成完整的三次握手
- **UDP扫描**：发送UDP数据包，探测UDP服务
- **Xmas扫描**：发送特殊标志位的数据包

**检测特征：**
- 来自同一源IP的大量不同目标端口的连接请求
- 连接尝试失败率高
- 扫描间隔短，自动化特征明显

### 1.3 DNS隧道
DNS隧道是一种利用DNS协议进行数据渗漏或命令控制的隐蔽通信方式。

**工作原理：**
- 将数据编码为DNS查询的子域名
- 通过DNS服务器解析过程传递数据
- 隐蔽性强，通常不会被防火墙拦截

**检测特征：**
- 异常长的DNS查询域名
- DNS查询频率异常高
- 大量不同的子域名查询
- DNS响应包大小异常

### 1.4 RST风暴
RST风暴是指大量TCP RST（复位）数据包，通常由网络攻击或异常网络行为引起。

**常见原因：**
- TCP连接被恶意中断
- 防火墙或IDS的主动干预
- 网络设备故障
- 半开连接攻击的副作用

**检测特征：**
- 短时间内大量RST包
- RST包与正常SYN/ACK比例异常
- 来源IP分散或集中

## 二、入侵检测系统（IDS）

### 2.1 什么是IDS
入侵检测系统（Intrusion Detection System）是一种通过监控网络流量或系统活动，识别可疑行为和已知攻击模式的安全系统。

### 2.2 IDS类型
- **网络IDS（NIDS）**：监控网络流量，分析数据包
- **主机IDS（HIDS）**：监控单台主机的系统日志和文件变化
- **协议IDS**：分析特定协议的异常行为

### 2.3 检测方法
- **特征检测**：匹配已知攻击的特征签名
- **异常检测**：建立正常行为基线，检测偏离基线的行为
- **协议分析**：解析协议结构，检测协议滥用

## 三、流量分析基础

### 3.1 什么是PCAP
PCAP（Packet Capture）是网络数据包捕获的文件格式，包含了网络接口上传输的原始数据包。

### 3.2 常用工具
- **Wireshark**：图形化网络协议分析工具
- **tcpdump**：命令行数据包捕获工具
- **tshark**：Wireshark的命令行版本

### 3.3 关键指标
- **包数（Packets）**：捕获的数据包总数
- **字节数（Bytes）**：捕获的数据总大小
- **流数（Flows）**：唯一的网络连接数
- **会话数（Sessions）**：完整的双向通信会话

## 四、AI辅助网络安全

### 4.1 机器学习在IDS中的应用
- **监督学习**：使用标记的攻击/正常样本训练分类器
- **无监督学习**：使用聚类、孤立森林等算法检测异常
- **半监督学习**：结合少量标记数据和大量无标记数据

### 4.2 常见算法
- **逻辑回归**：简单高效的二分类算法
- **随机森林**：集成学习，抗过拟合
- **XGBoost / LightGBM**：梯度提升树，性能优异
- **孤立森林**：无监督异常检测，适合高维数据
- **自编码器**：深度学习异常检测，捕捉复杂模式

### 4.3 特征工程
网络流量分析常用特征：
- 包大小统计（均值、方差、分位数）
- 包间隔时间统计
- 协议分布
- 端口分布
- 字节数统计
- 连接持续时间
- 上下行字节比

## 五、安全建议

### 5.1 防御措施
- **防火墙**：过滤恶意流量
- **入侵防御系统（IPS）**：实时阻断攻击
- **流量监控**：持续监控异常流量
- **定期扫描**：及时发现漏洞
- **最小权限原则**：限制账户权限

### 5.2 应急响应
1. **识别**：确认攻击发生
2. **遏制**：隔离受影响系统
3. **根除**：清除攻击痕迹
4. **恢复**：恢复系统正常运行
5. **总结**：分析原因，改进防御
